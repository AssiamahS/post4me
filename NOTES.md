- 2026-08-25: Composio CLI prints every id as `<REDACTED>` when env `CI=true` (GitHub Actions sets it) — the reel container id got parsed as the literal string and publish failed with "Invalid creation_id format". Fix: run `composio execute` with `CI=false` (publish_reel.py sets it; story step in post-approved.yml has `CI: "false"`).
- 2026-08-25: INSTAGRAM_POST_IG_USER_MEDIA_PUBLISH caps `max_wait_seconds` at 300 (schema max) — 600 fails input validation before the API is hit.
- 2026-08-25: ffmpeg `crop`/`zoompan` can't take `t` in w/h; slow zoom = `scale=w=iw*(1+k*t/T):h=...:eval=frame` then `crop=1080:1920`. Concat demuxer paths resolve relative to the list file — write absolute paths.
- 2026-08-25: edge-tts default `boundary="SentenceBoundary"` → zero WordBoundary events; pass `boundary="WordBoundary"` for caption timing.
- 2026-09-12: reels now carry a subject photo. Commons full-text search (`gsrsearch=name`) returns photos that merely mention the artist in the description (Lyn Collins → a random DJ) — use `intitle:` and require every subject word in the file title. Wikipedia REST `page/summary` `originalimage` is the reliable path; strip the `?utm_*` query before parsing the file name. `claude -p` tags 30 entries in ~60s; months 3-4 of the roadmap were never generated (cursor hits month 3 ~late Oct).

- 2026-09-16 approval texter: chat.db query had no is_from_me filter, so the cover.jpg the texter sent decoded to U+FFFC and was parsed as a NO with feedback 108s after the prompt (issue #46). Any iMessage reader must filter is_from_me=0 and drop attachment-only bodies.
- 2026-09-16 reach: the same C.R.E.A.M. mp4 posted from the phone app (DcdVwp9j88Y, 700 views/553 reach/10 likes) beat the Graph-API copy 7h later (DceGAWZDJJ-, 127/73/0) 5.5x. Every API-posted reel since sits at 75–179 views. Posting path matters as much as content; test app-posting (or Trial Reels, needs 1k followers) for the sample lane.
- 2026-09-16 sample lane: yt-dlp global config (~/.config/yt-dlp/config) has --match-filter duration>120 and --download-archive on the DJ library — scripts must pass --ignore-config (+ --remote-components ejs:github) or shorts and already-owned songs silently skip.
- 2026-09-17 iMessage attachments: Messages.app sandbox (macOS 15+) silently drops any 'send POSIX file' outside ~/Library/Messages or ~/Pictures (osascript exits 0; chat.db error 25 / transfer_state 6). Every cover/mp4 the texter sent before today never left the Mac. approval_texter.stage() now copies into ~/Library/Messages/.send-staging first.
- 2026-09-17 disk: a sample-reel render (8 video sections + 9 segments + staged mp4s) pushed the Mac to 0 bytes free mid-ffmpeg; even the Claude harness could not write tool output. sample_reel now deletes segments/overlays after concat and cached video sections older than a day; ~/Library/Messages/Attachments is 2.2GB of the user's own history, not ours.
- 2026-09-17 approvals: iMessage self-thread drops some Mac-sent videos on the phone (chat.db says delivered); the lgtm review page (:8794, now serves mp4 with Range) is the reliable channel; every draft goes to both.
